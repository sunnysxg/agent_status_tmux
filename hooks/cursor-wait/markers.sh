# Fixed Cursor CLI wait-dialog phrases (two cases only).
# Sourced by idle-scan.sh. Prefer unique option lines over titles.
# shellcheck disable=SC2034

CURSOR_WAIT_MARKERS=(
  # Plan → Agent
  "Approve mode switch (y)"
  # Ready to build?
  "Yes, build locally (b)"
)
